# HEEEHABABY 雛型環境變數範本
# 複製為 .env 後填入實際值。實際值不得進版本控制。

PORT=3000

# 資料庫 —— Supabase → Connect → Transaction pooler（port 6543）
# 本機測試請改用 scripts/pg-harness.mjs 印出的位址，不要連正式專案。
DATABASE_URL=
DB_POOL_MAX=3

# 簽章金鑰 —— 僅存於伺服器端 (README §10.2 / F-16)
QR_SIGNING_KEY=

# 後台登入 token 簽發金鑰 (雛型用；正式版改為 LIFF ID Token / Supabase JWT 驗證)
SESSION_SIGNING_KEY=

# 後台登入的共用密碼。沒設就整個登不進去（刻意 fail closed，不會變成誰都進得去）。
# 請用 16 字以上的隨機字串。這是 LINE Login 上線前的過渡措施。
ADMIN_LOGIN_PASSWORD=

# 預設匯率 JPY -> TWD，會寫入 settings.fx_jpy_twd 初始值
FX_JPY_TWD=0.215

# 收據圖片暫存位置（選填）。未設定時本機寫入 data/uploads，Vercel 寫入 /tmp。
UPLOAD_DIR=

# Supabase Storage —— 客人照片的私有存放處（bucket 由 006_ocr_pipeline.sql 建立）
# SUPABASE_URL 不是機密；SUPABASE_SERVICE_KEY 是，只放伺服器端，絕不可進前端或對話。
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
# SUPABASE_BUCKET=cart-images

# LINE Login channel ID —— 買家 LIFF 的 ID Token 就是用它核發的
# 沒設的話買家 API 一律 503（fail closed，不會退化成不驗身分）
LINE_LOGIN_CHANNEL_ID=

# 後台 LINE 登入：在同一個 LINE Login channel 底下另開一個 LIFF app，
# Endpoint URL 設成後台首頁（https://japan-system-lilac.vercel.app/），scope 勾 openid＋profile。
# LIFF ID 不是機密。沒設的話後台只有共用密碼入口。
ADMIN_LIFF_ID=
# LINE 登入確認可用之後設成 off，關掉後台的共用密碼入口，只留 LINE 一扇門。
# ADMIN_PASSWORD_LOGIN=off

# 綠界金流。HashKey／HashIV 只在伺服器端，絕不可出現在前端或 API 回應。
# 沒設就不開放線上刷卡（回 503）。測試請用綠界官方測試商店的參數。
ECPAY_MERCHANT_ID=
ECPAY_HASH_KEY=
ECPAY_HASH_IV=
# 正式：https://payment.ecpay.com.tw/Cashier/AioCheckOut/V5
# 測試：https://payment-stage.ecpay.com.tw/Cashier/AioCheckOut/V5
ECPAY_API_URL=
ECPAY_RETURN_URL=

# 通知佇列 —— n8n 取件時要帶的共用金鑰（X-Notify-Token），請用隨機長字串
# 這把金鑰同時是 GET /api/v1/health 詳細版的通行證，n8n 巡檢時帶同一個 header
NOTIFY_SHARED_SECRET=
# 後端排隊後戳 n8n 的網址（選填）。沒設也不影響出貨，通知會等 n8n 輪詢
NOTIFY_HOOK_URL=

# ---- 以下為正式串接時才需要，雛型未使用 ----
# N8N_BASE_URL=
# N8N_API_KEY=
# GS_SPREADSHEET_ID=
# GS_CREDENTIAL_ID=
# LINE_CREDENTIAL_ID=
# LINE_CHANNEL_SECRET=
# LINE_CHANNEL_ACCESS_TOKEN=
# LINE_OA_ID=
# OWNER_LINE_USER_ID=
# OPENAI_CREDENTIAL_ID=
# GEMINI_API_KEY=
# DRIVE_CREDENTIAL_ID=
# ERROR_WORKFLOW_ID=
